@{
    ViewData["Title"] = "Thiết Lập Admin";
    Layout = "_Layout";
}

@section Styles {
    <style>
        .setup-container {
            max-width: 600px;
            margin: 50px auto;
            padding: 30px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        
        .setup-header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .setup-header h1 {
            color: #2c3e50;
            margin-bottom: 10px;
        }
        
        .setup-header p {
            color: #7f8c8d;
            font-size: 16px;
        }
        
        .admin-info {
            background: #f8f9fa;
            border: 1px solid #e9ecef;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        
        .admin-info h4 {
            color: #28a745;
            margin-bottom: 15px;
        }
        
        .info-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding: 8px 0;
            border-bottom: 1px solid #eee;
        }
        
        .info-item:last-child {
            border-bottom: none;
        }
        
        .info-label {
            font-weight: bold;
            color: #495057;
        }
        
        .info-value {
            color: #6c757d;
            font-family: monospace;
        }
        
        .btn-setup {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            color: white;
            padding: 12px 30px;
            border-radius: 25px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            margin-top: 20px;
        }
        
        .btn-setup:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }
        
        .btn-login {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            border: none;
            color: white;
            padding: 12px 30px;
            border-radius: 25px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            margin-top: 20px;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }
        
        .btn-login:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            color: white;
            text-decoration: none;
        }
        
        .alert {
            padding: 15px;
            border-radius: 8px;
            margin: 20px 0;
        }
        
        .alert-success {
            background-color: #d4edda;
            border: 1px solid #c3e6cb;
            color: #155724;
        }
        
        .alert-danger {
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
            color: #721c24;
        }
        
        .loading {
            display: none;
            text-align: center;
            margin: 20px 0;
        }
        
        .spinner {
            border: 4px solid #f3f3f3;
            border-top: 4px solid #3498db;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            animation: spin 1s linear infinite;
            margin: 0 auto 10px;
        }
        
        @@keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
}

<div class="setup-container">
    <div class="setup-header">
        <h1>🔧 Thiết Lập Hệ Thống</h1>
        <p>Thiết lập tài khoản quản trị viên cho hệ thống</p>
    </div>

    @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
    {
        <div class="alert alert-danger">
            <strong>Lỗi:</strong> @ViewBag.ErrorMessage
        </div>
    }

    @if (ViewBag.AdminExists == true)
    {
        <div class="admin-info">
            <h4>✅ Tài khoản Admin đã tồn tại</h4>
            <div class="info-item">
                <span class="info-label">Tên đăng nhập:</span>
                <span class="info-value">@ViewBag.AdminInfo.Username</span>
            </div>
            <div class="info-item">
                <span class="info-label">Email:</span>
                <span class="info-value">@ViewBag.AdminInfo.Email</span>
            </div>
            <div class="info-item">
                <span class="info-label">Họ tên:</span>
                <span class="info-value">@ViewBag.AdminInfo.FullName</span>
            </div>
            <div class="info-item">
                <span class="info-label">Mật khẩu mặc định:</span>
                <span class="info-value">admin123</span>
            </div>
        </div>
        
        <a href="/Account/Login" class="btn-login">
            <i class="fas fa-sign-in-alt me-2"></i>Đăng Nhập Admin
        </a>
    }
    else
    {
        <div class="admin-info">
            <h4>⚠️ Chưa có tài khoản Admin</h4>
            <p>Hệ thống chưa có tài khoản quản trị viên. Vui lòng nhấn nút bên dưới để tạo tài khoản admin mặc định.</p>
        </div>
        
        <button id="setupAdminBtn" class="btn-setup" onclick="setupAdmin()">
            <i class="fas fa-user-shield me-2"></i>Tạo Tài Khoản Admin
        </button>
    }

    <div id="loading" class="loading">
        <div class="spinner"></div>
        <p>Đang thiết lập hệ thống...</p>
    </div>

    <div id="result" style="display: none;"></div>
</div>

@section Scripts {
    <script>
        function setupAdmin() {
            const btn = document.getElementById('setupAdminBtn');
            const loading = document.getElementById('loading');
            const result = document.getElementById('result');
            
            // Ẩn nút và hiển thị loading
            btn.style.display = 'none';
            loading.style.display = 'block';
            result.style.display = 'none';
            
            // Gọi API tạo admin
            fetch('/Admin/CreateDefaultAdmin')
                .then(response => response.json())
                .then(data => {
                    loading.style.display = 'none';
                    result.style.display = 'block';
                    
                    if (data.success) {
                        result.innerHTML = `
                            <div class="alert alert-success">
                                <h4>✅ Thiết lập thành công!</h4>
                                <p><strong>${data.message}</strong></p>
                                <div class="admin-info">
                                    <h5>Thông tin đăng nhập:</h5>
                                    <div class="info-item">
                                        <span class="info-label">Tên đăng nhập:</span>
                                        <span class="info-value">${data.credentials.username}</span>
                                    </div>
                                    <div class="info-item">
                                        <span class="info-label">Mật khẩu:</span>
                                        <span class="info-value">${data.credentials.password}</span>
                                    </div>
                                </div>
                                <a href="${data.loginUrl}" class="btn-login">
                                    <i class="fas fa-sign-in-alt me-2"></i>Đăng Nhập Ngay
                                </a>
                            </div>
                        `;
                    } else {
                        result.innerHTML = `
                            <div class="alert alert-danger">
                                <h4>❌ Có lỗi xảy ra</h4>
                                <p><strong>${data.message}</strong></p>
                                ${data.existingAdmin ? `
                                    <div class="admin-info">
                                        <h5>Admin hiện tại:</h5>
                                        <div class="info-item">
                                            <span class="info-label">Tên đăng nhập:</span>
                                            <span class="info-value">${data.existingAdmin.username}</span>
                                        </div>
                                        <div class="info-item">
                                            <span class="info-label">Email:</span>
                                            <span class="info-value">${data.existingAdmin.email}</span>
                                        </div>
                                    </div>
                                ` : ''}
                                <button class="btn-setup" onclick="location.reload()">
                                    <i class="fas fa-refresh me-2"></i>Thử Lại
                                </button>
                            </div>
                        `;
                    }
                })
                .catch(error => {
                    loading.style.display = 'none';
                    result.style.display = 'block';
                    result.innerHTML = `
                        <div class="alert alert-danger">
                            <h4>❌ Lỗi kết nối</h4>
                            <p>Có lỗi xảy ra khi kết nối đến server: ${error.message}</p>
                            <button class="btn-setup" onclick="location.reload()">
                                <i class="fas fa-refresh me-2"></i>Thử Lại
                            </button>
                        </div>
                    `;
                });
        }

        // Tự động thiết lập nếu chưa có admin
        document.addEventListener('DOMContentLoaded', function() {
            @if (ViewBag.AdminExists != true)
            {
                <text>
                // Tự động thiết lập sau 2 giây
                setTimeout(() => {
                    if (document.getElementById('setupAdminBtn').style.display !== 'none') {
                        setupAdmin();
                    }
                }, 2000);
                </text>
            }
        });
    </script>
} 